<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.isen.tinco.androiderestaurant">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidERestaurant"
        android:usesCleartextTraffic="true">
        <activity android:name="fr.isen.tinco.androiderestaurant.User.UserActivity"></activity>
        <activity android:name="fr.isen.tinco.androiderestaurant.basket.BasketActivity" />
        <activity android:name="fr.isen.tinco.androiderestaurant.Detail.DetailActivity" />
        <activity android:name="fr.isen.tinco.androiderestaurant.CategoryActivity" />
        <activity android:name="fr.isen.tinco.androiderestaurant.HomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>